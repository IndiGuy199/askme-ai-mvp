const { createClient } = require('@supabase/supabase-js'); const dotenv = require('dotenv'); dotenv.config(); async function checkMemorySummary() { const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE); const { data, error } = await supabase.from('user_profiles').select('user_id, memory_summary').eq('user_id', '6ac69871-d613-420b-9a25-7e5d175f92b3').single(); if (error) { console.error('Error fetching memory summary:', error); return; } console.log('Memory Summary:'); console.log(data.memory_summary); } checkMemorySummary();
